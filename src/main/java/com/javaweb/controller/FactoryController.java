package com.javaweb.controller;

import java.util.ArrayList;

import javax.servlet.http.HttpSession;

import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.RestController;



@RestController
public class FactoryController {
	
	/*FactoryService factService;

@ModelAttribute("listFactory")
public ArrayList<Factory> addListFormationToModel(HttpSession session){
	Factory fac=(Factory) session.getAttribute("factory");
	return formService.findByFactory(fac);*/
}
